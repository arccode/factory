{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "BuildConfigDirectory": {
      "description": "A build-specific directory to load config schema and overrides default values.",
      "type": "string"
    },
    "RuntimeConfigDirectory": {
      "description": "A directory for configurations generated or downloaded at runtime that overrides all (build config or default config) values.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "BuildConfigDirectory",
    "RuntimeConfigDirectory"
  ]
}
