This folder contains implementations for different DUT boards.

To add your own class, inherit from one board and override your DUT
properties:

 from cros.factory.test.dut import board
 from cros.factory.test.dut import component
 from cros.factory.test.dut import power

 class MyPower(power.Power):
   def CheckACPresent(self):
     return False

 class MyBoard(board.DUTBoard):

   @DUTProperty
   def power(self):
     return MyPower(self)
