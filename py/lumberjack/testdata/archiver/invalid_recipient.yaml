data_types:
  regcode:
    archived_dir: archives/
    project: mocked_project
    notes: template that contains only regcode for unittest
    duration: hourly
    source_file: raw/mocked_regcode.csv
    # The recipient is not correct.
    encrypt_key_pair: [keys/unittest_crosreg.pub, do-evil]
