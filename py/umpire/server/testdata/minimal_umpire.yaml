rulesets:
- bundle_id: test
  note: ruleset for test
  active: true
  enable_update:
    test_image: # Note that we should be able to dump [~, RUNIN]
    - null
    - RUNIN
services:
  http:
    reverse_proxies:
    - proxy_addr: 192.168.51.1:8080
      remoteip: 192.168.51.0/24
  rsync: {}  # empty parameter
  dhcp:
    interface: # Note that we should be able to dump [eth4] instead.
    - eth4
    netmask: 255.255.0.0
    range: # Note that we should be able to dump [10.3.11.50, 10.3.50.254].
    - 10.3.11.50
    - 10.3.50.254
    subnet: 10.3.0.0
  shop_floor: {}
bundles:
- id: test
  note: bundle for test
  payloads: payload.99914b932bd37a50b983c5e7c90ae93b.json
